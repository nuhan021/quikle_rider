import 'package:flutter/material.dart';

class VerificationCheck extends StatelessWidget {
  const VerificationCheck({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold();
  }
}